//TESTANDO PUSH

// function inicializada toda vez que clicar em Criar Quizz
function criarQuizz() {
    // Botão grande --> caso não tem nenhum quizz e pequeno caso já existam quizzes 
    let botãoGrande = document.querySelector('.nenhum-quizz'); //Quando não há quizzes --> Aparece o botão grande.
    let seusQuizzes = document.querySelector('.seus-Quizzes'); //Quando HÁ QUIZZES ISSO APARECE

    // Ao clicar em "Criar Quizz" ou no "+" essa tela deve sumir, dando lugar à tela de Tela 3: Criação de Quiz
}